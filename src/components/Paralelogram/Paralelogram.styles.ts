import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  parallelogramInner: {
    position: 'absolute',
    left: 0,
    top: 0,
  },
  parallelogramRight: {
    top: 0,
    position: 'absolute',
  },
  parallelogramLeft: {
    top: 0,
    position: 'absolute',
  },
});

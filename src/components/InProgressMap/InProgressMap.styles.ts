import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  containerMap: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 16,
    marginBottom: 24,
  },
  containerAway: {
    flex: 1,
  },
  containerTextAway: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  textDistance: {
    fontSize: 16,
    marginLeft: 8,
    flex: 1,
    flexWrap: 'wrap',
  },
});

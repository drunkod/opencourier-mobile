import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 48,
    alignSelf: 'stretch',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 12,
    flexDirection: 'row',
  },
  textTitle: {
    fontSize: 16,
    fontWeight: '800',
    lineHeight: 24,
  },
  icon: {
    width: 20,
    height: 20,
    marginRight: 10,
  },
});
